<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidComponents">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidComponents">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
    IMPLICIT INTENT RECEIVER — SecondActivity
    ══════════════════════════════════════════
    Registers SecondActivity as a target for apps that want to share images.
    Android reads this intent-filter at runtime to determine if this
    Activity can handle an incoming implicit intent.

    android:exported="true"
        Required when using intent-filters — allows external apps
        (Chrome, Gallery, etc.) to launch this Activity.

    android:launchMode="singleTop"
        If SecondActivity is already at the top of the back stack,
        Android reuses the existing instance and calls onNewIntent()
        instead of creating a new one.

    intent-filter:
        Tells Android "this Activity can handle the following intent"

        ACTION_SEND
            → The sender wants to share/send data to another component.

        CATEGORY_DEFAULT
            → Required for any Activity that receives implicit intents.
              Without it, Android will never route intents to this Activity.

        mimeType="image/*"
            → Narrows the filter to image sharing only (jpg, png, gif, etc.)
              Text, video, or other types will NOT match this Activity.

    Full Flow:
        User shares image in Chrome
            → Android scans all installed apps for ACTION_SEND + image/*
            → Finds this Activity via intent-filter
            → Launches SecondActivity with image URI in EXTRA_STREAM
-->
        <activity
            android:name=".intent.SecondActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
    </application>
    <!--
    MANIFEST QUERIES — Package Visibility
    ──────────────────────────────────────
    Android 11+ (API 30) restricts which apps your app can "see"
    by default. Without <queries>, resolveActivity() always returns
    null — even if an email app IS installed.

    This block tells Android:
    "My app needs to know about apps that handle ACTION_SEND with text/plain"

    Without it  → resolveActivity() returns null → intent never fires
    With it     → Android grants visibility → resolveActivity() works correctly
-->
    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>